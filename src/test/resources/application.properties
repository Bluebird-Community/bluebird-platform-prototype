# Each test should use a testcontainer and a dedicated postgres instance rather than using h2.
# We may use h2 at a later stage, but I found that more often h2 lacks the functionality postgres provides
# which results in a weird mix of testcontainers and h2 at the same time, being incompatible to each other
# It needs to be decided with route to go.
#spring.datasource.url=jdbc:h2:mem:bluebird-symbiont;MODE=PostgreSQL
#spring.datasource.driverClassName=org.h2.Driver
#spring.datasource.username=sa
#spring.datasource.password=password
#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# By default, we create-drop by hibernate for now, but may use liquibase later for the tests => slower
spring.jpa.hibernate.ddl-auto=create-drop

# TODO MVR this should work out of the box, without OpenNMS being present in general.
# Maybe OpenNMs needs to send us these things in advance, when the producer is started
org.bluebird.integration.opennms.home=${user.home}/dev/opennms/target/opennms-34.0.0-SNAPSHOT/

org.bluebird.alarm.propagation.enabled=false